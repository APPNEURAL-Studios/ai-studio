{
  "name": "@appneural/ai-studio",
  "version": "0.1.0",
  "private": true,
  "description": "AI Studio — served at ai.studio.appneural.com",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  },
  "engines": {
    "node": ">=20"
  }
}
